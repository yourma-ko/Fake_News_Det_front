# 🚀 Быстрый старт FakeCheck Web

## Шаг 1: Настройка Backend URL

Откройте файл `src/environments/environment.development.ts` и укажите URL вашего Backend API:

```typescript
export const environment = {
  production: false,
  apiBaseUrl: 'https://localhost:5001', // ЗАМЕНИТЕ НА ВАШ URL
  swaggerUrl: 'https://localhost:5001/swagger/v1/swagger.json'
};
```

## Шаг 2: Установка зависимостей (если ещё не установлены)

```powershell
npm install
```

## Шаг 3: Запуск приложения

```powershell
ng serve
```

Приложение будет доступно: **http://localhost:4200**

## 🎯 Маршруты

- `/` - Главная страница (Landing)
- `/register` - Регистрация
- `/login` - Вход
- `/app/verify` - Проверка новостей (требует аутентификации)
- `/app/history` - История проверок (требует аутентификации)

## 🔐 Тестирование

1. Перейдите на `http://localhost:4200`
2. Нажмите "Начать бесплатно" или "Зарегистрироваться"
3. Заполните форму регистрации
4. После успешной регистрации вы автоматически войдете в систему
5. Перейдите в раздел "Проверка"
6. Введите URL новости или текст для проверки
7. Посмотрите результат

## 📝 Пример данных для теста

### Регистрация
```json
{
  "email": "test@example.com",
  "password": "Test123!",
  "name": "Тестовый Пользователь"
}
```

### Проверка по URL
```
https://example.com/news/some-article
```

### Проверка по тексту
```
Введите любой текст новости для проверки достоверности.
Минимум 10 символов.
```

## ⚠️ Важно

1. **Backend должен быть запущен** перед использованием фронтенда
2. **CORS должен быть настроен** на Backend для `http://localhost:4200`
3. **SSL сертификат**: Если Backend использует HTTPS с самоподписанным сертификатом, добавьте исключение в браузере

## 🔧 Возможные проблемы

### Ошибка CORS
Добавьте в Backend (`Program.cs`):
```csharp
builder.Services.AddCors(options =>
{
    options.AddDefaultPolicy(policy =>
    {
        policy.WithOrigins("http://localhost:4200")
              .AllowAnyHeader()
              .AllowAnyMethod()
              .AllowCredentials();
    });
});

app.UseCors();
```

### Ошибка подключения к API
Проверьте:
- Backend запущен
- URL в `environment.development.ts` правильный
- Порт Backend соответствует указанному

### Ошибка 401 Unauthorized
- Проверьте, что JWT токен правильно добавляется в заголовки
- Откройте Developer Tools > Network > Headers
- Должен быть заголовок: `Authorization: Bearer <token>`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте вкладку Network для HTTP запросов
3. Убедитесь, что Backend возвращает правильные ответы

---

**Готово! Приложение должно работать.** 🎉
